{% block menu %}
<header>
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <!-- <a class="navbar-brand" href="#">Project name</a> -->
            </div>
            <div id="navbar" class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li><a href="{{ route('home') }}"><i class="glyphicon glyphicon-home"></i></a></li>
                    <li role="presentation" class="dropdown">
                        <a class="dropdown-toggle disabled" data-toggle="dropdown"
                            href="{{ route('inventory') }}" role="button"
                            aria-haspopup="true" aria-expanded="false"
                        >
                            Inventory <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ route('product.edit') }}">Move to Packship</a></li>
                            <li><a href="{{ route('product.index', {inventorySlug: 'on-hands'}) }}">On Hands Inventory</a></li>
                            <li><a href="{{ route('product.index', {inventorySlug: 'consignment'}) }}">Consignment Inventory</a></li>
                            <!-- <li><a href="">Update a product's status</a></li> -->
                            <li><a href="{{ route('product.index', {inventorySlug: 'sales'}) }}">Sales</a></li>
                        </ul>
                    </li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li>
                        <a href="#" class="visible-xs" onclick="$('#logout-form').submit();">Logout</a>
                        <form class="form-inline" method="POST" id="logout-form" action="{{ route('logout') }}">
                            {{ csrf_field() }}
                            <div class="navbar-btn visible-sm visible-md visible-lg">
                                <button type="submit" class="btn btn-danger">Logout</button>
                            </div>
                        </form>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>
{% endblock %}
